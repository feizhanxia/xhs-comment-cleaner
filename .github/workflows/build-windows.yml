name: Build Windows EXE

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    name: Windows 11 compatible EXE
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v7.0.1

      - name: Set up Python 3.12
        uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements-dev.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Compile-check Python source
        run: python -m compileall -q app tests

      - name: Build windowed onefile EXE
        run: python -m PyInstaller --noconfirm --clean XHSCommentCleaner.spec

      - name: Verify sync Playwright is absent from EXE
        shell: pwsh
        run: |
          $archive = (pyi-archive_viewer -r ".\dist\XHSCommentCleaner.exe" | Out-String)
          if ($archive -notmatch "playwright\.async_api") {
            throw "Async Playwright API is missing from the packaged EXE"
          }
          if ($archive -match "playwright\.sync_api|playwright\._impl\._sync_base") {
            throw "Sync Playwright API must not be packaged"
          }

      - name: Smoke-test packaged GUI
        shell: pwsh
        run: |
          $env:QT_QPA_PLATFORM = "offscreen"
          $process = Start-Process -FilePath ".\dist\XHSCommentCleaner.exe" -ArgumentList "--smoke-test" -Wait -PassThru
          if ($process.ExitCode -ne 0) {
            throw "Packaged EXE smoke test failed with exit code $($process.ExitCode)"
          }

      - name: Smoke-test async Playwright with system Edge
        shell: pwsh
        run: |
          $env:QT_QPA_PLATFORM = "offscreen"
          $process = Start-Process -FilePath ".\dist\XHSCommentCleaner.exe" -ArgumentList "--browser-smoke-test" -Wait -PassThru
          if ($process.ExitCode -ne 0) {
            Get-Content "$env:LOCALAPPDATA\XHSCommentCleaner\logs\app.log" -ErrorAction SilentlyContinue
            Get-Content "$env:LOCALAPPDATA\XHSCommentCleaner\logs\crash.log" -ErrorAction SilentlyContinue
            throw "Async Playwright Edge smoke test failed with exit code $($process.ExitCode)"
          }

      - name: Generate SHA-256 checksum
        shell: pwsh
        run: |
          $hash = (Get-FileHash ".\dist\XHSCommentCleaner.exe" -Algorithm SHA256).Hash.ToLower()
          "$hash  XHSCommentCleaner.exe" | Set-Content -Encoding ascii ".\dist\XHSCommentCleaner.exe.sha256"
          Get-Item ".\dist\XHSCommentCleaner.exe" | Format-List Name,Length,LastWriteTime
          Get-Content ".\dist\XHSCommentCleaner.exe.sha256"

      - name: Upload downloadable EXE
        uses: actions/upload-artifact@v7.0.1
        with:
          name: XHSCommentCleaner-Windows-x64
          path: |
            dist/XHSCommentCleaner.exe
            dist/XHSCommentCleaner.exe.sha256
          if-no-files-found: error
          retention-days: 30
          compression-level: 0

      - name: Publish tagged GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$env:GITHUB_REF_NAME" `
            ".\dist\XHSCommentCleaner.exe#XHSCommentCleaner.exe" `
            ".\dist\XHSCommentCleaner.exe.sha256#XHSCommentCleaner.exe.sha256" `
            --title "XHS Comment Cleaner $env:GITHUB_REF_NAME" `
            --generate-notes
